We have our piece array for logic.
Now we need to use a read array for hardware inputs.

Register/update (and display to test) gameBoard continuously
Don't forget to empty/delete pointer? set nullptr? for the tile you move away from

If a piece moves we send the x1,y1,x2,y2 values to the logic array using coordinates[4]
From here we can validate the move and update the logic array.

ADD MULTI THREADING

We do NOT need to update the read array, cause the whole point is that it just reads.
The piece array stores the game state, somehow.


-start condition is being checked



DONE
--------------------------
At start check if the correct tiles read white or black and start game.
Do readBoard like we've been doing.
