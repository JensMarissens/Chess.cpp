#include <stdio.h>
#include "gameLogic.h"

gameLogic::gameLogic() {}

bool gameLogic::validMove()
{
}

/*void registerMove()
{
    if (original[i] != current[i])
    {
        std::cout << "Value at index " << i << " has changed from "
                  << original[i] << " to " << current[i] << std::endl;

                  
                  For every move 2 tiles should change.
                  You can use that to add the piece and square to PGN AND to check if the move was valid.
    }
}*/